<div class="card mb-4">
	<a href="{{ path('property.show', {slug: property.slug, id: property.id}) }}" style="text-decoration:none">
		{% if property.filename %}
			<img src="{{ vich_uploader_asset(property, 'imageFile') | imagine_filter('thumb') }}" class="card-img-top" style="width: 100%; height: auto">
		{% else %}
			<img src="{{ '/images/properties/empty.jpg' | imagine_filter('thumb') }}" class="card-img-top" style="width: 100%; height: auto">
		{% endif %}
		<div class="card-body">
			<h5 class="card-title">
				{{ property.title }}
			</h5>
			<h6 class="card-subtitle mb-2">{{ property.city }}
				({{ property.postalCode }})</h6>
			{% if app.user %}
				<a href="{{ path('admin.property.edit', {id: property.id}) }}" class="btn btn-primary" style="--bs-btn-padding-y: .25rem; --bs-btn-padding-x: .5rem; --bs-btn-font-size: .75rem;">Editer</a>
			{% endif %}
			<p class="card-text">{{property.surface}}m²</p>
			<div class="text-primary" style="font-size:2rem; font-weight: bold;">
				{{ property.formattedPrice }}
				€
			</div>
		</div>
	</a>
</div>
